spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
line:
  liff-id: test-liff-id
  channel-id: test-channel-id
  channel-secret: test-channel-secret
  issuer: https://access.line.me
  friendship-check-enabled: true
